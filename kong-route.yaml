apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: kong-gateway
  namespace: hotel-dev
spec:
  port:
    targetPort: proxy
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  to:
    kind: Service
    name: kong-gateway